{
  "name": "teaboom-test",
  "version": "1.0.0",
  "private": true,
  "description": "Верхняя часть карточки товара Teaboom.ru — тестовое задание",
  "scripts": {
    "build:css": "sass scss/styles.scss css/styles.css --no-source-map",
    "watch:css": "sass --watch scss/styles.scss css/styles.css --no-source-map",
    "start": "npx --yes serve -l 4173 ."
  },
  "devDependencies": {
    "sass": "^1.79.0"
  }
}
